apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "discogs-client.fullname" . }}-test-connection"
  labels:
    {{- include "discogs-client.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "discogs-client.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
