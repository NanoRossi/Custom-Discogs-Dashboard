apiVersion: v1
kind: Service
metadata:
  name: {{ include "discogs-client.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
  selector:
    app: {{ include "discogs-client.name" . }}
