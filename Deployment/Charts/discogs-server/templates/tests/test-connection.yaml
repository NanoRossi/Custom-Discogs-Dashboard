apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "discogs-server.fullname" . }}-test-connection"
  labels:
    {{- include "discogs-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "discogs-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
